# Technical Debt Register

| ID | Debt | Cause | Interest | Remediation trigger | Owner | State |
|---|---|---|---|---|---|---|
| DEBT-004 | **Found 2026-09-18 while verifying DEBT-001: a Claude Code runtime file, `.claude/scheduled_tasks.lock`, dirties the worktree and makes `activate_lease.py` refuse activation.** `python scripts/validate_os.py` failed `schema-3 interactive lease activation must pass: ERROR: Git worktree must be clean before lease activation`. The human's diagnostic run (a temporary, reverted edit to the failure message in `.claude/hooks/common.py`, then `validate_os.py`) printed the cause: `?? .claude/scheduled_tasks.lock`. The failure was identical before and after the DEBT-001 patch, so it is unrelated to encoding. Why the file stays untracked inside the validator's temporary copy is not explained. | The Claude Code app writes this lock file into the project's `.claude/` while a session with scheduled tasks is open; `.gitignore` had no entry for it | Because `activate_lease.py` requires a clean worktree (`git status --porcelain=v1 --untracked-files=all` must print nothing), the same file can block real lease activation while the app is open, with a message that hides the actual cause | Mitigation applied by the human on 2026-09-18, uncommitted: `.claude/scheduled_tasks.lock` appended to `.gitignore`; `validate_os.py` then passed (`Checks: 169`, `RESULT: PASS`). Remaining: commit the `.gitignore` change; explain why the file was untracked in the validator's copy (open) | Human (applied in own terminal) | Mitigated, uncommitted; root explanation open |
| DEBT-003 | **Found 2026-09-18 (raised by an independent reviewer of `TASK-STATE-TRANSITION-001`, then verified directly against the code): a lease that expires or is deactivated before sealing cannot be re-activated, and therefore its implementation can no longer be sealed through the normal scripts.** `scripts/activate_lease.py:176-178` calls `git_worktree_clean()` (`.claude/hooks/common.py:165-177`), which runs `git status --porcelain=v1 --untracked-files=all` and fails on ANY output. `scripts/seal_implementation.py:37-38` calls `load_lease()`, which rejects an expired, inactive or missing lease. But implementation files written under a lease are, by definition, untracked/uncommitted until after sealing. So once the lease is gone, the only two states are: implementation files still uncommitted (activation refuses, "Git worktree must be clean") or committed (nothing left for `seal_implementation.py` to seal — `"there is no implementation diff to seal"`). Observed live: `TASK-SCHEMA-001`'s lease expired at `2026-09-17T05:12:49Z` (8 h) before it was sealed. Related trap: `seal_implementation.py:43-45` rejects the seal if ANY changed path lies outside `allowed_paths`, yet agents are permitted to edit `docs/**` while a lease is active — every docs edit made during the lease window silently makes the later seal fail | The scripts assume "activate → implement → seal" happens as one uninterrupted, docs-quiet session inside the 8-hour lease; no recovery path was designed for an overrun, a deactivation, or docs work made under an active lease | Any implementation that outlives its lease, or is accompanied by docs edits, needs a manual recovery each time (below) and risks a mis-sequenced seal | Workaround (human, own terminal, no code change): commit every change except the implementation directory; move the implementation files out of the worktree (e.g. `Move-Item schemas ..\schemas-hold`); activate a fresh lease; move the files back; seal immediately, with no other worktree edit in between. Proper fix (human-only, `CONTROLLED_PATHS`): let `activate_lease.py` tolerate dirty paths that fall inside the task's own `allowed_paths`, or add an explicit re-lease/extend path, or allow sealing an expired lease; and/or add a process rule that agents make no docs edits while a lease is active | Tech owner (human-only — `scripts/activate_lease.py`, `scripts/seal_implementation.py`, `.claude/hooks/common.py` are `CONTROLLED_PATHS`) | Open |
| DEBT-002 | **Found 2026-09-18, during independent review of `TASK-CAPABILITY-001`'s draft contract**: `scripts/activate_lease.py`'s write path (`destination.write_text(...)`, ~line 228) never checks whether `.ai-governance/implementation-lease.json` already exists before overwriting it — no `destination.exists()` check, no read of the existing `state`/`task_id`. Confirmed live: `TASK-SCHEMA-001`'s lease (`expires_at: 2026-09-17T05:12:49Z`) is now actually past its expiry (today is 2026-09-18) but the file on disk still literally reads `"state": "ACTIVE"` — nothing rewrites that field on expiry. `.claude/hooks/common.py:load_lease()` (used to gate ordinary governed tool calls) does correctly check `expires_at` against the current time at line 209, so an agent's routine Bash/Write calls are not fooled by the stale label — but `activate_lease.py`'s own activation path has no equivalent check, so a human could run it for a second task right now and silently clobber the first task's lease record with no warning, expired or not | `activate_lease.py` was written assuming a human would only ever run it when no other lease exists; nothing enforces that assumption in code | If two task contracts are ever activated back-to-back without the first being sealed/deactivated first, the audit trail for the first (including a possibly `ACCEPT`'d-but-unsealed implementation, exactly `TASK-SCHEMA-001`'s current state) is silently lost | Add a guard to `activate_lease.py`: refuse to activate a new lease if `.ai-governance/implementation-lease.json` exists and its `state` is `ACTIVE` and unexpired, unless `--force` (or similar) is passed; separately, have it (or `load_lease()`) rewrite `state` to `EXPIRED` once `expires_at` has passed, so the file on disk doesn't keep asserting `ACTIVE` after the fact | Tech owner (human-only — `scripts/activate_lease.py` is `CONTROLLED_PATHS`) | Open |
| DEBT-001 | **ESCALATED 2026-09-16 — actually blocks lease activation, not just a preflight warning.** Same root-cause bug exists in TWO places: `scripts/doctor.py:57-61` (originally found) AND `.claude/hooks/common.py:git_state():137-145` (found when `python scripts/activate_lease.py` genuinely failed with `"AI Game Development OS must be installed at the Git repository root"` on a real activation attempt). `git_state()` is called directly by `scripts/activate_lease.py:173` and by `load_lease()` (used on every governed hook call) — so this repository cannot activate or sustain ANY implementation lease until fixed, regardless of task. **2026-09-16, confirmed workaround**: user ran `$env:PYTHONUTF8=1; python scripts/doctor.py --require-claude` in their own PowerShell terminal — flips `RESULT: FAIL` to `RESULT: PASS`, no file edit needed. **2026-09-17, workaround scope found narrower than assumed**: the human then successfully activated the `TASK-SCHEMA-001` lease with `PYTHONUTF8=1` set in that same terminal (`ACTIVE: TASK-SCHEMA-001 until 2026-09-17T05:12:49Z`, `.ai-governance/implementation-lease.json` confirmed present) — but Claude's own first `Write` call to an `allowed_paths` file under that active lease was still denied with the identical DEBT-001 error (`LEASE_REQUIRED: ... must be installed at the Git repository root`). Root cause: `govern_write.py`'s own lease-validation step calls the same buggy `git_state()`, and it runs in the process that hosts Claude Code's own hook execution — which never inherits an env var set only inside the human's separate interactive PowerShell session. **So `PYTHONUTF8=1` set per-terminal only unblocks human-run scripts in that terminal; it does not unblock Claude's own governed tool calls under an otherwise-valid lease.** For that, `PYTHONUTF8` would need to be set as a persistent user/system environment variable (e.g. `setx PYTHONUTF8 1`) *and* the Claude Code app restarted so its own hook-hosting process inherits it — untried, unconfirmed. The permanent code fix (adding `encoding="utf-8"` directly) sidesteps this inheritance problem entirely and remains the recommended real fix | `subprocess.run(..., text=True)` in both files does not pass `encoding="utf-8"`, so git's UTF-8 `rev-parse --show-toplevel` output can decode differently from `Path.resolve()` on Windows for this repository's non-ASCII path (`oyungeliştirme`), breaking the string/path equality check | Every non-ASCII-path Windows install of this package is currently unable to activate any lease at all without the `PYTHONUTF8=1` workaround — this is a hard functional block absent that env var, not a cosmetic warning | **P0 in code, but operationally unblocked via confirmed workaround.** Add `encoding="utf-8"` to every `subprocess.run(...)` call in both `scripts/doctor.py` and `.claude/hooks/common.py`'s `git_state()`/`git_worktree_clean()`/related functions, so the fix does not depend on every human remembering to set `PYTHONUTF8=1` | Tech owner (human-only — both files are `CONTROLLED_PATHS`, no agent can ever edit them) | **Fix applied by the human on 2026-09-18, not yet committed (commit SHA pending).** Patch: `encoding="utf-8", errors="replace"` added to every `text=True` `subprocess.run` call (`common.py` 4, `doctor.py` 4, `activate_lease.py` 1, `validate_os.py` 10, per the human's `git diff --stat`). **Evidence 2026-09-18** (human's own PowerShell, Windows 11, Python 3.13.7, `PYTHONUTF8` removed from the session): `python scripts/doctor.py` → `RESULT: PASS` and the `package must be installed at the Git repository root` warning no longer appears (it appeared before the patch); `python scripts/validate_os.py` → `Checks: 169`, `RESULT: PASS` (after the DEBT-004 mitigation). **Not yet shown:** independent review of the patch; a non-ASCII-path case in `validate_os.py`'s adversarial suite; a live check that Claude's own governed `Write` succeeds under an active lease without `PYTHONUTF8`; removal of the persistent `setx PYTHONUTF8` value. State: Fix applied, verification incomplete |

Debt is an explicit trade-off, not a label for every imperfection. Record why it exists, what it costs, and when it must be paid.

