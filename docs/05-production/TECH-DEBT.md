# Technical Debt Register

| ID | Debt | Cause | Interest | Remediation trigger | Owner | State |
|---|---|---|---|---|---|---|
| DEBT-001 | **ESCALATED 2026-09-16 — actually blocks lease activation, not just a preflight warning.** Same root-cause bug exists in TWO places: `scripts/doctor.py:57-61` (originally found) AND `.claude/hooks/common.py:git_state():137-145` (found when `python scripts/activate_lease.py` genuinely failed with `"AI Game Development OS must be installed at the Git repository root"` on a real activation attempt). `git_state()` is called directly by `scripts/activate_lease.py:173` and by `load_lease()` (used on every governed hook call) — so this repository cannot activate or sustain ANY implementation lease until fixed, regardless of task | `subprocess.run(..., text=True)` in both files does not pass `encoding="utf-8"`, so git's UTF-8 `rev-parse --show-toplevel` output can decode differently from `Path.resolve()` on Windows for this repository's non-ASCII path (`oyungeliştirme`), breaking the string/path equality check | Every non-ASCII-path Windows install of this package is currently unable to activate any lease at all — this is a hard functional block, not a cosmetic warning | **P0 — fix before attempting any lease activation on this machine again.** Add `encoding="utf-8"` to every `subprocess.run(...)` call in both `scripts/doctor.py` and `.claude/hooks/common.py`'s `git_state()`/`git_worktree_clean()`/related functions | Tech owner (human-only — both files are `CONTROLLED_PATHS`, no agent can ever edit them) | Open |

Debt is an explicit trade-off, not a label for every imperfection. Record why it exists, what it costs, and when it must be paid.

