# Animation Standard

Status: `PROPOSED`

Define skeleton authority, scale, retargeting, root motion, locomotion ownership, state machine, montage/action contracts, sockets, additive layers, network prediction/replication, first-person representation, ragdoll recovery, event/notifies, deformation acceptance poses, LOD, and performance budget.

Changing a frozen skeleton, bone name, socket, or retargeting base is an expensive decision requiring blast-radius analysis and migration.

